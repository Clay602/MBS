# Project Instructions

此文件包含此專案的團隊共享架構、規範、工作流程和其他存放庫指南。

## 規範
- 使用繁體中文進行溝通。
- 遵循專案現有的編碼風格。
- **自動重啟規範**：以後每次修改完成後，都必須先關閉舊的程式實例（如果存在），然後啟動新的腳本或編譯後的 `.exe` 進行測試。
- **路徑限制規範**：僅能在 `D:\07_Linux\MBS` 及其子路徑（如 `Release`）進行檔案新增、修改、移除與執行測試指令，嚴禁在此路徑範圍之外新增、修改、刪除任何專案相關檔案或執行測試。
- **Git 發布路徑規範**：以後所有 Git 版本控制與發布推送操作（例如 `git commit`、`git push` 等），都必須且僅能在 `D:\07_Linux\MBS` 目錄下進行，嚴禁在 `D:\07_Linux\tmppppp` 或是其他非本專案路徑下執行任何 Git 指令。

---

# Modbus 測試工具開發進度 (截至 2026-06-17 v4.3)

> [!NOTE]
> 截至 2026-06-17，第四階段實作 (v4.3 Phase 4) 已完成程式原始碼混淆保護與發布反編譯安全性測試。藉由導入 python-obfuscator 代碼混淆機制對關鍵變數、邏輯進行深度文字級混淆與結構重整，並在 PyInstaller 打包時動態注入 hiddenimports 以防止依賴流失。最終產出具有高度防反編譯能力的 exe 可執行檔並通過運行測試，達成安全防護系統的完美收尾。

### 1. 技術架構
- **名稱**: Acer ABC Modbus Tool
- **架構版本**: v4.3 (第四階段 - 程式混淆保護與發布反編譯安全性測試版)
- **核心技術**:
    - **封裝設計**: 引入 `ModbusTabPage` 類別，獨立管理各設備的連線狀態、通訊 Client、暫存器快取與 UI 表格。
    - **多任務處理**: 支援多個設備同時連線與獨立的自動讀取 (Polling) 背景執行緒。
    - **連線共享技術**: 實作引用計數與共用互斥鎖（`comm_lock`）機制，支援跨分頁相同串口參數之連線共用。
    - **動態地址解碼**: 解碼函式支援傳入絕對位址，解決讀取起始地址偏移（例如狀態區 `8192` 起始）時查無標籤設定的 Bug。

---

### 2. 已實作功能 (截至 v4.3)

- **v4.3 第四階段 — 程式混淆保護與發布反編譯安全性測試**:
    - **文字級別混淆**：導入 `python-obfuscator` 工具，對 `modbus_tool.py` 原始碼中的變數、類別與函式名稱進行文字混淆，並將程式碼壓縮解密執行，防止程式碼被反編譯直觀閱讀。
    - **一鍵打包指令碼**：建立 `build_obf.py`，支援自動備份原始碼、安裝/驗證依賴、程式混淆、動態修改 PyInstaller 規格檔中的 `hiddenimports`（解決 `exec` 混淆導致外部庫如 `pymodbus`、`serial` 丟失問題），並在打包完後自動還原原始檔，完全不污染開發環境。
    - **軟體說明與對話框同步**：同步更新主程式版本號、操作說明及版本資訊對話框內容，發布為 `v4.3`。
- **v4.2 第三階段 — 關鍵讀寫入口之多點分散授權校驗**:
    - **核心讀取防禦**：在核心讀取函數 `perform_read` 發送 Modbus 實體通訊前秒速校驗授權。若無效，自動關閉 Polling 並記錄錯誤 log；手動點擊讀取則彈窗阻斷提示。
    - **核心寫入防禦**：在核心寫入函數 `perform_write` 執行 Modbus 寫入前校驗授權。若無效，寫入錯誤 log、彈出錯誤視窗提示並直接中止寫入，回傳 `False`。
    - **授權資訊視窗優化**：縮小標題字型至 13 級（原 16 級），並於右下角提供「📂 載入授權檔案 (license.lic)」按鈕，支援在運行中直接載入授權、寫入本機並當場重新驗證 RSA 授權與動態更新 UI 狀態（成功為綠色，失敗為橘色/紅色）。
- **v4.1 第二階段 — 用戶端啟動攔截與授權啟用對話框**:
    - **啟動阻斷機制**：於程式啟動時先將主畫面徹底隱藏 (`root.withdraw`)。若本機授權校驗未通過，主視窗將保持隱藏狀態阻斷未授權用戶，若使用者點選取消或關閉視窗則調用 `sys.exit(0)` 安全退出。
    - **授權回填與啟用對話框**：開發了精美的 `LicenseActivationDialog` 啟用介面。提供本機 16 碼機器碼 Entry（唯讀，Consolas 字型）及一鍵複製按鈕；提供「載入授權檔案」按鈕，引導使用者匯入 `license.lic`。
    - **自動驗證與存檔放行**：匯入檔案後，程式在背景以 RSA 公鑰自動進行解密驗證，通過後自動將合法的授權寫入並保存於程式執行目錄，隨即提示成功並 `root.deiconify` 顯示主視窗進入主程式。
    - **已授權無縫載入**：已授權用戶啟動時不會看到任何啟用視窗，無延遲直接秒入 Modbus 主畫面。
- **v4.0 第一階段 — RSA 離線授權與管理端 GUI 工具**:
    - **獨立授權產生 GUI 工具**：開發了 [license_generator.py](file:///d:/07_Linux/MBS/license_generator.py)，提供 Tkinter 現代圖形介面，具備隨機生成 1024-bit RSA 金鑰對、複製公鑰硬編碼、格式化輸入 16 碼機器碼與快速配置截止日期（整合了精簡的「📅」與「♾️」兩個圖示按鈕並排於輸入框右側，彈出的行事曆支援單箭頭「◀/▶」切換年份與「‹/›」切換月份並配有「回到今天」），一鍵產出 `license.lic`。
    - **無依賴 RSA 大數演算庫**：完全採用純 Python 實現 Miller-Rabin 質數測試、擴展歐幾里得模反元素及大數模冪運算，避開第三方加密庫依賴，確保打包 EXE 體積零膨脹。
    - **強固機器碼偵測**：於用戶端 `modbus_tool.py` 中實作 BIOS UUID 與 MachineGuid 拼接之 SHA-256 16 碼機器碼產生機制，設有 WMIC 異常捕捉與 fallback 降級機制，保證設備識別之唯一性與穩定性。
    - **授權校驗與資訊對話框**：主程式 `modbus_tool.py` 內置 RSA 公鑰參數與 Base64 還原解密算法，自動在執行檔目錄（支援開發環境 fallback）尋找並校驗 `license.lic` 的合法性與有效期限。同時於「說明」選單新增「授權資訊」選項，方便用戶查詢/複製本機機器碼，並展示最新授權狀態。
- **方案 B 重命名無腳本背景更新與自我清理 (v3.8)**:
    - **重命名佔用替換**：在 Windows 平台上，利用執行中 EXE 可重命名的特性，更新時將當前執行檔 `Acer_ABC_Modbus_Tool.exe` 重命名為 `Acer_ABC_Modbus_Tool.old`，然後將新下載的暫存檔直接命名為原檔名。
    - **原生對話框與自動重啟**：重命名後透過 `subprocess.Popen` 以無黑視窗模式重新拉起新版主程式，隨即結束舊程式。新版程式在偵測到更新參數時，透過原生的 Tkinter 彈出提示，完全免去批次檔或 PowerShell 彈窗。
    - **非同步備份清理**：新版程式啟動後，利用背景執行緒非同步多次嘗試刪除 `Acer_ABC_Modbus_Tool.old`（以防舊進程釋放檔案鎖延遲），清理過程完全不阻塞主 UI。
- **自動背景隱密更新與安全校驗 (v3.7)**:
    - **非同步 HTTPS 下載與進度 UI**：在背景非同步比對 version.json，檢測到新版時彈出更新對話框（顯示 Changelog），點擊「立即更新」可呈現分塊下載的進度條與百分比。加入 Chrome User-Agent 偽裝以穿透企業防火牆。
    - **SHA-256 完整性校驗**：下載完成後自動計算 SHA-256 雜湊，比對一致後才執行覆蓋程序，並設有防覆蓋原始碼的防呆機制。
    - **VBScript 隱藏通道背景覆蓋**：寫入 `updater.bat` 與 `updater.vbs` 腳本，透過 Windows Shell 背景執行 VBS 來拉起批次覆蓋，**100% 隱藏 CMD 黑色視窗**，覆蓋成功後彈出提示，並自動將下載的暫存檔與兩個腳本完全自刪清理乾淨。
- **全新大 M 霓虹晶片去背圖示與 Base64 嵌入 (v3.6)**:
    - **大 M 霓虹晶片圖示**：為程式全新設計並導入高科技感的大 M 霓虹晶片圖示，使主旨更突出易辨。
    - **四角漫水去背 (Flood Fill)**：在轉檔時，使用 Python Pillow 的 `ImageDraw.floodfill` 漫水去背演算法，從圖像四個角落對接近白色的像素進行填充，將晶片圓角外圍白色背景完全洗成完全透明色 `(0, 0, 0, 0)`。
    - **多尺寸圖示規格打包**：產生打包包含 **`16x16`, `32x32`, `48x48`, `64x64`, `128x128`, `256x256`** 所有標準 Windows 解析度規格的透明 ICO 檔編譯入 EXE 中，解決了 Windows 在不同檢視規格與工作列下因缺少小圖示而無法載入的問題。
    - **Base64 硬編碼嵌入**：將去背後的 64x64 圖示以 Base64 編碼，硬編碼嵌入主程式中的 `icon_data` 變數，使 Python 主程式啟動時可獨立載入視窗圖示，擺脫外部 ICO 檔案依賴。
    - **懸浮提示不超出主視窗**：優化了按鈕懸浮提示（ToolTip）的顯示位置計算，透過 `winfo_toplevel()` 自動獲取主視窗的螢幕座標範圍與寬高，強制限制 ToolTip 的彈出坐標，100% 確保提示詞內容完全保留在主視窗範圍之內，不會超出主視窗或電腦螢幕邊界。
- **Serial 監控關鍵字搜尋與各 Entry Enter 鍵觸發 (v3.6)**:
    - **監控日誌即時搜尋**：於 Serial 模式頂部控制列新增搜尋 Entry 框，輸入關鍵字即時全域高亮（淺黃色背景），目前焦點以深橘色背景標示，並支援 `🔼` / `🔽` 導覽切換與自動滾動定位。
    - **搜尋與發送 Enter 鍵觸發**：在搜尋輸入框中按 Enter 自動切換定位到下一個匹配；在發送欄按 Enter 自動送出數據，功能互不干涉。
    - **寫入對話框 Enter 鍵觸發**：在寫入對話框的「起始地址」、「寫入大小」或「寫入值」輸入框中按下 Enter，即可直接執行寫入，並會自動進行資料格式的合法性校驗與防錯。
    - **自動滾動開關控制**：於 Serial 模式頂部控制列新增 `📜` 按鈕（開啟狀態，懸浮提示「自動滾動已開啟」）與 `🚫` 按鈕（關閉狀態，懸浮提示「自動滾動已關閉」）。當關閉自動滾動時，接收新日誌或切換顯示格式將不再自動強制滾動視窗，便於使用者自由向上滾動、閱讀與複製歷史記錄。
    - **UI 按鈕等距排版優化**：調整頂部控制列左側按鈕的 `padx` 為 `5`，使左側控制按鈕群組（ASCII 🔤、HEX 🔢、時間戳 🕒、自動滾動 📜/🚫）的相鄰間距與右側按鈕群組（🧹、📄、🧱）完全統一為 **10 像素**，使整體介面更加精美對稱。
    - **滑鼠右鍵複製與貼上選單**：為唯讀日誌監控視窗 `serial_log` 實作選取文字後滑鼠右鍵點擊彈出「複製」選單功能；同時為 `search_entry` 搜尋框及 `send_entry` 發送框實作滑鼠右鍵彈出「貼上」選單（包含剪下/複製功能），並支援貼上搜尋詞後即時自動高亮搜尋結果，極大提升鍵盤與滑鼠聯動的便捷性。
- **拖曳分頁標籤動態排序與批次渲染優化 (v3.5)**:
    - **滑鼠即時拖曳**: 支援以滑鼠左鍵按住設備分頁標籤並左右拖曳，標籤會隨滑鼠即時更換位置，提供流暢的視覺體驗。
    - **佈局自動儲存**: 拖曳排序結束後，系統會自動將最新的分頁順序儲存至設定檔，確保下次重啟程式時能完美還原最新佈局。
    - **Serial 監控批次渲染**: 導入歷史日誌同步批次渲染，解決切換 HEX/ASCII 與時間戳時事件佇列卡死之效能問題，延遲降至 50ms 以下，並修復磁碟日誌重複寫入 Bug。
- **ASCII 自訂型態與解碼修復 (v3.4)**:
    - **多暫存器 ASCII 讀取**: 支援將連續多個暫存器解碼為 ASCII 可讀字串，預設大小端維持 `Little-Endian Byte-Swap (CDAB)`。
    - **自訂暫存器大小**: 點位設定中新增 `ascii_size` 參數，在讀取時自動跳過相應的暫存器表格渲染。
    - **零值格式化優化**: 當讀取的 ASCII 暫存器資料皆為 0 時，Parsed 欄位直接呈現為空字串，且不使用點（`.`）佔位。
    - **地址偏移解碼修復**: 修正當點位起始地址非 0（例如 `system.mcuUUID` 地址為 `8192`）時，因索引偏移導致查不到標籤設定使解碼長度降級為 1 的 Bug。
- **ASCII 寫入控制與自動補零 (v3.4)**:
    - **型態自動適配**: 對 ASCII 型態的點位雙擊或滑鼠右鍵點擊寫入時，寫入控制視窗會自動識別資料型態為 `ASCII`，且長度大於 1 個暫存器時自動採用 `16 — Write Multiple Registers` 功能碼。
    - **填寫長度與補零**: 寫入對話框開放讓使用者自訂寫入大小，當輸入的字串長度小於寫入大小時，程式會在不足的部分自動補零（`\x00`），預設填入該點位的暫存器長度。
- **右鍵選單複製欄位數值 (v3.4)**:
    - **儲存格位置識別**: 在 Treeview 上點選滑鼠右鍵時，會依據滑鼠目前所指到的欄位（點位名稱、地址、Hex、Dec、解碼值）動態彈出選單。
    - **一鍵複製**: 提供「複製 [欄位名稱] ([數值])」功能，點選後將該欄位數值直接寫入系統剪貼簿，並在底部的系統日誌輸出提示。
- **打包 Windows 可執行檔 (v3.4)**:
    - 已使用 PyInstaller 成功將主程式編譯為獨立運行的 `.exe` 執行檔，免除在未安裝 Python 環境之 Windows 電腦上的部署阻礙。
- **64-bit 數據解碼與寫入 (v2.3)**:
    - 支援 64-bit Int (有號)、64-bit UInt (無號) 與 64-bit Float (雙精度浮點數) 的拼接解碼，支援 Big/Little Endian 各種排列 (ABCD, DCBA, CDAB, BADC)。
    - 重構表格渲染，引入 `skip_count` 計數器，在啟用 64-bit 時自動折疊並跳過後續 3 個點位的表格列渲染。
- **工具列捷徑與位置優化 (v2.3)**:
    - 工具列 Separator 後方新增 **「連線參數設定 (🔌)」** 與 **「讀取參數設定 (⚙️)」** 捷徑圖示，並完成對調以符合人體工學動線。
    - 新增分頁按鈕圖示升級為 **「書籤分頁 (📑)」**，並調整至工具列的第一順位。
- **選單擴展與狀態同步 (v2.3)**:
    - 「設定 (S)」選單下新增 **「寫入參數設定」** 選項，且選單項目「寫入控制」統一更名為「寫入參數設定」。
    - 實作選單項目與當前 active 分頁狀態的同步：無活躍分頁時，連線、讀取與寫入選單自動禁用。
- **操作說明同步更新 (v2.3)**:
    - 選單「說明 (H) -> 操作說明」對話框內引導文字已全部同步更新，納入新圖示（📑, 🔌, ⚙️, ✏️）與對應捷徑之操作描述。
- **多分頁管理介面**:
    - **Notebook 容器**: 使用 `ttk.Notebook` 組織多個設備分頁。
    - **分頁操作**: 支援點選 **`📑`** 新增分頁；支援標籤右鍵「重新命名」與「關閉分頁」；支援雙擊標籤快速重新命名。
- **分頁獨立設定檔儲存 (v2.2)**:
    - 點選 💾 可單獨將當前分頁的通訊與讀取點位參數另存為獨立的 JSON 檔案，分頁名稱自動同步為該檔名。
    - 實作防重複載入機制，檢測到已在其他分頁載入相同 JSON 檔案時，會跳出警告並拒絕。
    - 全域 `last_settings.json` 自動儲存所有開啟分頁的 metadata 以及設定檔路徑，重啟時可完全還原。
- **跨分頁同參數連線共用 (v2.2)**:
    - 當切換至未連線分頁且發現與其他已連線分頁使用相同的連線參數時，程式會自動繼承共用該通訊 Client 與鎖，以防通訊衝突。
    - 中斷連線時採取同步中斷管理，關閉實體連接埠的同時會將其他所有共用該 Client 的分頁同步設為未連線狀態並停止其 Polling。
- **寫入 (Write) 控制介面 (v2.1)**:
    - 雙擊 Treeview 的 `Hex / Dec / parsed` 欄位可自動帶入對應的地址、功能碼與格式開啟寫入視窗。
    - 新增 Windows 風格的「寫入成功後自動關閉視窗」原生勾選框，並免去重複的二次確認詢問框。
- **RTU 通訊增強**:
    - 各分頁可獨立設定 Parity、Data Bits 與 Stop Bits。
- **連線設定視窗優化**:
    - **防坍塌機制**: 切換 TCP/RTU 模式時幾何尺寸自動垂直適應，防止視窗收縮消失。
    - **欄位水平對齊**: 統一所有參數欄位的 grid layout 對齊與輸入元件 `padx`/`sticky`。
    - **序列埠智慧選取**: RTU 模式下串口自動偵測與智慧預設選取。
- **字體大小與排版調整 (v2.1)**:
    - 全域預設字型字體放大至 **11級**（微軟正黑體 11），包括 Treeview、表頭及分頁標籤，表格行高 rowheight 增加至 28。
    - 系統日誌區文字維持 **Consolas 10**，保留偵錯閱讀空間。
- **新增說明 (Help) 選單與讀取統計顯示區 (v2.2)**:
    - 選單列最右側加入「說明 (H)」選單，內含「操作說明」與「版本資訊」對話框。
    - 各分頁標籤右端新增「讀取統計 — 成功: X | 失敗: Y | 總計: Z」顯示面板，並可一鍵 `[重設]` 歸零。
- **自動化與穩定性**:
    - **DPI 與佈局**: 延續 v1.8 的高度自適應對話框與 Windows DPI 感知。

---

### 3. 專案檔案結構
- [modbus_tool.py](file:///d:/07_Linux/MBS/modbus_tool.py) : 主程式 (v4.3 最新版)
- [build_obf.py](file:///d:/07_Linux/MBS/build_obf.py) : v4.3 一鍵混淆與打包自動化指令碼 [NEW]
- [modbus_tool_v4.3.py](file:///d:/07_Linux/MBS/modbus_tool_v4.3.py) : v4.3 穩定備份版 [NEW]
- [modbus_tool_v4.2.py](file:///d:/07_Linux/MBS/modbus_tool_v4.2.py) : v4.2 穩定備份版
- [modbus_tool_v4.1.py](file:///d:/07_Linux/MBS/modbus_tool_v4.1.py) : v4.1 穩定備份版
- [modbus_tool_v4.0.py](file:///d:/07_Linux/MBS/modbus_tool_v4.0.py) : v4.0 穩定備份版
- [modbus_tool_v3.7.py](file:///d:/07_Linux/MBS/modbus_tool_v3.7.py) : v3.7 穩定備份版
- [modbus_tool_v3.6.py](file:///d:/07_Linux/MBS/modbus_tool_v3.6.py) : v3.6 穩定備份版
- [modbus_tool_v3.5.py](file:///d:/07_Linux/MBS/modbus_tool_v3.5.py) : v3.5 穩定備份版
- [dist/Acer_ABC_Modbus_Tool.exe](file:///d:/07_Linux/MBS/dist/Acer_ABC_Modbus_Tool.exe) : 打包後的 Windows 獨立執行檔 (統一命名最新版)
- [pfc_vars.json](file:///d:/07_Linux/MBS/pfc_vars.json) : 點位設定檔，起始地址為 0
- [pfc_status.json](file:///d:/07_Linux/MBS/pfc_status.json) : 狀態點位設定檔，起始地址為 8192
- `last_settings.json` : 全域多分頁記憶與設定檔路徑紀錄檔
- `tcp_slave_sim.py` / `rtu_slave_sim.py` : 測試用模擬器
- [GEMINI.md](file:///d:/07_Linux/MBS/GEMINI.md) : 專案共享開發規範與進度檔 (此文件)

---

### 4. 待辦/建議優化
- 繪製趨勢圖 (Trend Chart) 功能。
- **規劃中大版本 (v4.0) — 離線授權與安全防護系統**：
    - **雙端離線授權**：
        - 用戶端主程式偵測到未授權時彈出授權視窗，顯示本機 UUID 雜湊後之 16 碼機器碼。
        - 獨立開發管理員 UI 產生器 [license_generator.py](file:///d:/07_Linux/MBS/license_generator.py)，供管理員輸入機器碼與截止日期，以 RSA 私鑰加密產生授權碼。
        - 用戶端內置公鑰解密校驗，通過後寫入 `license.lic` 檔放行。
    - **安全防護機制**：
        - 導入 python-obfuscator 對代碼進行文字混淆，防止編譯後的 exe 被反編譯還原 (v4.3 已完成)。
        - 於核心讀寫功能 `perform_read` 與 `perform_write` 內置多點分散授權校驗，防止單點 Bypass (v4.2 已完成)。
    - **分階段導入計畫**：
        - **第一階段**：完成 RSA 演算校驗庫開發與管理員 UI 工具 (已完成)。
        - **第二階段**：對接用戶端啟動攔截與授權回填介面 (已完成)。
        - **第三階段**：導入關鍵讀寫入口之多點分散授權校驗 (已完成)。
        - **第四階段**：執行程式代碼混淆打包與發布反編譯安全性測試 (已完成，改用 python-obfuscator 實現)。
